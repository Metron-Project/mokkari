.. _install:

Installation
============

**mokkari** requires Python >= 3.8. It has no external dependencies.

Installing/Upgrading from the PyPI
----------------------------------

To install the latest stable version from the PyPI:

::

    $ pip3 install -U --user mokkari
